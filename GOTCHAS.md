# GOTCHAS

## 맥에서 zip을 풀면 한글 파일명이 NFD로 바뀌어 문자열 매칭이 전부 어긋난다 (2026-09-11)

**증상**: 9_sandbox를 맥에서 zip으로 풀어 실행하면, 챗봇 문서 선택 시 `config.py`의
`EXAMPLE_QUESTIONS`에 등록된 문서인데도 문서별 예시 질문이 안 뜨고 기본 예시로 폴백됨.

**근본 원인**: macOS(HFS+/압축 해제 유틸)는 한글 파일명을 NFD(자모 분리형, `가` → `ㄱ+ㅏ`)로
저장한다. `os.listdir()`가 NFD 문자열을 돌려주면 소스 코드에 적힌 NFC 키와
**눈에는 같아 보여도 코드포인트가 달라** `dict` 조회·`==` 비교가 전부 실패한다.
파일명이 데이터(딕셔너리 키, 벡터디비 source 필드)로 쓰이는 순간 터지는 구조적 문제.

**교훈**:
- 한글 파일명을 키·식별자로 쓰는 코드는 **파일시스템 경계에서 반드시
  `unicodedata.normalize("NFC", ...)`로 통일**할 것 (listdir 결과, 업로드 파일명, 쿼리 파라미터).
- 반대로 디스크 조회 시엔 NFC 이름으로 NFD 파일을 못 찾을 수 있으므로(리눅스),
  listdir을 순회하며 NFC 기준으로 대조하는 리졸버가 필요하다 (`rag._resolve`).
- 프런트엔드도 `String.normalize("NFC")`로 이중 방어 (JS 문자열 비교도 동일하게 어긋남).
- 윈도우(NTFS)는 저장된 그대로(NFC) 유지라 이 문제가 없고, zip 한글 인코딩은
  UTF-8 플래그(macOS zip CLI 기본)면 Windows 10 1803+ 기본 해제에서 정상.

**적용 커밋**: 9_sandbox — rag.py `nfc()`/`_resolve()`, app.py 업로드 파일명 정규화,
index.html 예시 질문 키 정규화.
